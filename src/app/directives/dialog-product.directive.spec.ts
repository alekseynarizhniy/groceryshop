import { DialogProductDirective } from './dialog-product.directive';

describe('DialogProductDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogProductDirective();
    expect(directive).toBeTruthy();
  });
});
