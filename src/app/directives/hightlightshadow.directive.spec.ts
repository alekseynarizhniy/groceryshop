import { HightlightShadowDirective } from './hightlightshadow.directive';

describe('HightlightShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightShadowDirective();
    expect(directive).toBeTruthy();
  });
});
